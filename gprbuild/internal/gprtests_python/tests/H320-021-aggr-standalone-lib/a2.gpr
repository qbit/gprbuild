library project A2 is
   for Source_Dirs use ("a2_src");
   for Library_Name use "a2";
   for Library_Dir use "a2lib";
   for Object_Dir use "o2lib";
   for Library_Kind use "relocatable";

   package Compiler is
      for Default_Switches ("Ada") use ("-O2");
   end Compiler;
end A2;
