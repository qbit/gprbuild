with "prj";

project Main is
   for Source_Dirs use ("src");
   for Main use ("main.adb", "run.adb");

   for Exec_Dir use "bin";

   package Builder is
      for Executable ("main.adb") use "mymain";
   end Builder;
end Main;
