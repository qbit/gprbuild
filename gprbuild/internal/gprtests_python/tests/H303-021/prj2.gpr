project Prj2 is
   for Main use ("main2.adb");

   for Object_Dir use "obj";
   for Exec_Dir use ".";

   package Linker is
      for Switches ("Ada") use ("-Lc", "-ltoto");
   end Linker;
end Prj2;

