project Ext extends "orig/orig.gpr" is
   for Object_Dir use "obj";
   for Library_Name use "ext";
   for Library_Dir  use "lib";
   for Library_Kind use "relocatable";
   for Library_Interface use ("pkg");

   package Binder is
--      for Default_Switches ("Ada") use ("-v");
   end Binder;
end Ext;
