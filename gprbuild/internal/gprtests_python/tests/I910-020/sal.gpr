project SAL is
  for Source_Dirs use ("src");
  for Object_Dir use "obj";
  for Library_Dir use "lib";
  for Library_Name use "sal"; 
  for Library_Kind use "relocatable";
  for Library_Interface use ("Some_Package");

  package Compiler is
    for Default_Switches("Ada") use ("-g");
  end Compiler;

end SAL;
