library project Ancillary is 
    for Languages   use ("C");
    for Source_Dirs use (".");
    for Source_Files use (
                    "fd_recv.c",
                    "fd_send.c"
                    );

    for Library_Name use "ancillary";

    for Object_Dir use "obj";
    for Library_Dir use "lib";

    package Compiler is 
        for Default_Switches("C") use (
                                      "-Wall", "-Wextra", "-Wshadow", 
                                      "-O2", "-g",
                                      "-DNDEBUG"
                                      );
    end Compiler;

end Ancillary;

