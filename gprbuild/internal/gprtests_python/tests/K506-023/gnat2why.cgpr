configuration project Gnat2why is
   for Default_Language use "Ada";
   package Compiler is
      for Driver ("Ada") use "sh";
      for Required_Switches ("Ada") use ("compile.sh");
      for Object_File_Suffix ("Ada") use "__package.mlw";
      For Language_Kind ("Ada") use "unit_based";
      For Dependency_Kind ("Ada") use "Makefile";
      for Dependency_Switches ("Ada") use ();
   end Compiler;

   package Naming is
      for Spec_Suffix ("Ada") use ".ads";
      for Body_Suffix ("Ada") use ".adb";
      for Casing use "lowercase";
      for Dot_Replacement use "-";
   end Naming;
end Gnat2why;

